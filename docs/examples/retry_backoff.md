# Retry Backoff: Worked Examples

Exponential backoff with jitter, walked through for a three-attempt
policy. Includes the deterministic delay sequence produced under a
fixed seed so the behaviour can be reproduced in tests.
