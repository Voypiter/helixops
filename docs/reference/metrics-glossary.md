# Metrics Glossary and SLI Definitions

Defines every emitted metric, its unit, and how it maps to a service
level indicator: task latency percentiles, run throughput, retry rate,
and recovery cost. Intended as the contract for dashboards.
