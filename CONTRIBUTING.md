# Contributing to HelixOps

## Workflow
1. Branch from main.
2. Write tests first (TDD).
3. Keep commits focused and milestone-scoped.
4. All tests must pass before review.

## Conventions
- Type hints on all public functions.
- Docstrings follow Google style.
- No external data dependencies in tests.
